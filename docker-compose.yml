services:
  postgres:
    image: postgres:17-alpine
    restart: unless-stopped
    ports:
      - "5433:5432"
    environment:
      POSTGRES_USER: leadey
      POSTGRES_PASSWORD: leadey_dev
      POSTGRES_DB: leadey
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
